---
status: done
archived: 2026-07-19
---

# 코칭 v2.1 PR③ — R5 cross_session_claude_md Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** v2.1 스펙 §6.1의 R5 두 번째 subtype `cross_session_claude_md`를 구현한다 — 한 프로젝트의 여러 세션에서 반복해서 읽히는 참조 파일을 찾아 "CLAUDE.md에 넣어두면 매 세션 다시 안 읽어도 된다"는 조치 가능한 카드로 발화.

**Architecture:** 기존 `R5RepeatedRead`(PR②, `within_session_context_drift`만 채움)의 `evaluate`에 두 번째 탐지 pass를 더한다 — `file_read`를 `(host, project, target)`로 집계해 별개 세션 ≥3·읽기우세·노이즈 제외를 만족하는 파일을 프로젝트 카드 1장으로. 같은 `rule_id="R5"`지만 `subtype`·`dedup_key`가 달라 context_drift와 **공존**한다. 서사 표면(`finding_advice`)이 subtype별로 분기하고, 프론트(`coach-helpers`·`CoachTab`)가 subtype별 카드 제목을 낸다.

**Tech Stack:** Rust(크레이트 `agent-mentor` = core) · rusqlite/serde_json · Svelte 5 + vitest(프론트).

## Global Constraints

- **빌드(Windows, Git Bash) — 모든 cargo 호출 전 매번:**
  ```bash
  export PATH="$HOME/.cargo/bin:/c/Users/jibin/mingw64/mingw64/bin:$PATH"
  export CARGO_HTTP_CHECK_REVOKE=false
  export RUSTUP_TOOLCHAIN=stable-x86_64-pc-windows-gnu
  ```
- 코어 테스트: `cargo test -p agent-mentor <filter>`. 프론트: `npm run build` + `npx vitest run`.
- **정밀도의 선:** 추정 토큰은 서사에 "추정/잠재"로 명시. cross_session est는 **잠재(추정)** `Σ_file (distinct_sessions − 1) × 1200`.
- **공존(§6.3):** 새 cross_session finding은 `scope_kind="project"` + `dedup_key`에 `cross_session_claude_md` 포함 → `ops.rs`의 `delete_findings_by_rule_and_scope("R5","session")`에 안 걸리고 context_drift와 공존. **ops.rs·coach.rs 변경 없음**(R5는 이미 등록·fix_command None).
- **스코프(YAGNI):** §8.2 deref 프론트(`deref_jsonl_line` Tauri 커맨드 + SessionModal "미리보기→전문 보기")는 **드롭** — `get_session_transcript`(commands.rs:255)가 이미 세션 전문 열람을 제공해 중복. 코어 `ops::deref_jsonl_line`(PR① 완료)은 그대로 둔다. 이 PR③은 R5 cross_session 본체 + 그 카드 표면만.

## File Structure

| 파일 | 책임 | 태스크 |
|---|---|---|
| `crates/core/src/rules/r5_repeated_read.rs` | cross_session_claude_md 탐지 pass 추가(노이즈 제외·별개세션≥3·읽기우세) | 1 |
| `crates/core/src/diary/mod.rs` (`finding_advice`) | R5 arm을 subtype별로 분기(cross_session 문구) | 2 |
| `crates/core/src/coach.rs` (test only) | R5 cross_session evidence → fix_command None 확인 | 2 |
| `src/lib/ui/coach-helpers.ts` (신규 `coachTitle`) | rule_id+subtype → 카드 제목(순수 함수) | 3 |
| `src/lib/ui/coach-helpers.test.ts` (신규) | coachTitle vitest | 3 |
| `src/lib/ui/CoachTab.svelte` | 로컬 TITLE 맵 → `coachTitle(f.rule_id, f.evidence)` | 3 |

## Deferred (이번 밖)

- §8.2 deref 세션 상세 전문보기 — YAGNI 드롭(위 Global Constraints). 필요해지면 `get_session_transcript` 위에 얹거나 별도 백로그.
- 후속 R5 subtype 5종 세분(§9) — 관찰 지표용, 이번 미구현.

---

### Task 1: R5 cross_session_claude_md 탐지 (코어 룰)

**Files:**
- Modify: `crates/core/src/rules/r5_repeated_read.rs`

**Interfaces:**
- Consumes: `events` 테이블 `tool_call` 행(`tool_kind` ∈ file_read/file_edit/file_write, `tool_target`, `session_id`, `host`, `project_id`) — PR① 수집. `store.session_ctx(sid) -> Option<(_,_,Option<String> cwd,_)>`. `crate::finding::{Finding, Prescription, Severity}`.
- Produces: `R5RepeatedRead`에 필드 `claude_md_min_sessions: usize`(기본 3). `evaluate`가 context_drift findings에 더해 subtype `cross_session_claude_md` findings를 반환 — `scope_kind="project"`, `dedup_key="R5|{host}|{project}|cross_session_claude_md"`, `est=Σ(session_count−1)×1200`, prescription `{kind:"add_to_claude_md", payload:{files:[...]}}`, evidence `{subtype, user_actionability:"high", files:[{path,session_count}], session_ids, total_sessions, cwd}`.

- [ ] **Step 1: 실패 테스트 추가 (cross_session 발화 + 3개 음성 경계)**

`r5_repeated_read.rs`의 `#[cfg(test)] mod tests { ... }` 안, 마지막 테스트(`r5_needs_two_drift_sessions`) 뒤에 추가:

```rust
    #[test]
    fn r5_cross_session_claude_md_fires_on_3_sessions() {
        let store = SqliteStore::open_in_memory().unwrap();
        // 3개 별개 세션이 shared.rs를 각 1회 읽음(편집 없음) → context_drift 미발화, cross_session 발화
        let evs = vec![
            read("s1", 0, "shared.rs"),
            read("s2", 0, "shared.rs"),
            read("s3", 0, "shared.rs"),
        ];
        store.upsert_events(&evs).unwrap();
        let findings = R5RepeatedRead::default().evaluate(&store).unwrap();
        let cs: Vec<_> = findings.iter()
            .filter(|f| f.evidence["subtype"] == "cross_session_claude_md").collect();
        assert_eq!(cs.len(), 1);
        let f = cs[0];
        assert_eq!(f.scope_kind, "project");
        assert_eq!(f.dedup_key, "R5|Windows|d--proj|cross_session_claude_md");
        assert_eq!(f.evidence["user_actionability"], "high");
        assert_eq!(f.evidence["total_sessions"], 3);
        assert_eq!(f.evidence["files"][0]["path"], "shared.rs");
        assert_eq!(f.evidence["files"][0]["session_count"], 3);
        assert_eq!(f.est_tokens_saved, (3 - 1) * 1200);
        assert_eq!(f.prescription.as_ref().unwrap().kind, "add_to_claude_md");
    }

    #[test]
    fn r5_cross_session_excludes_noise_paths() {
        let store = SqliteStore::open_in_memory().unwrap();
        let evs = vec![
            read("s1", 0, "node_modules/dep/index.js"),
            read("s2", 0, "node_modules/dep/index.js"),
            read("s3", 0, "node_modules/dep/index.js"),
        ];
        store.upsert_events(&evs).unwrap();
        let findings = R5RepeatedRead::default().evaluate(&store).unwrap();
        assert!(findings.iter().all(|f| f.evidence["subtype"] != "cross_session_claude_md"));
    }

    #[test]
    fn r5_cross_session_suppressed_when_edits_dominate() {
        let store = SqliteStore::open_in_memory().unwrap();
        // 3개 세션에서 읽기 1 + 편집 1씩 → reads(3) > edits(3) 거짓 → WIP 파일, 미발화
        let sess = |s: &str| vec![read(s, 0, "wip.rs"), edit(s, 1, "wip.rs")];
        let mut evs = sess("s1"); evs.extend(sess("s2")); evs.extend(sess("s3"));
        store.upsert_events(&evs).unwrap();
        let findings = R5RepeatedRead::default().evaluate(&store).unwrap();
        assert!(findings.iter().all(|f| f.evidence["subtype"] != "cross_session_claude_md"));
    }

    #[test]
    fn r5_cross_session_needs_3_distinct_sessions() {
        let store = SqliteStore::open_in_memory().unwrap();
        let evs = vec![read("s1", 0, "ref.md"), read("s2", 0, "ref.md")];
        store.upsert_events(&evs).unwrap();
        let findings = R5RepeatedRead::default().evaluate(&store).unwrap();
        assert!(findings.iter().all(|f| f.evidence["subtype"] != "cross_session_claude_md"));
    }
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `cargo test -p agent-mentor r5_cross_session`
Expected: 컴파일은 되나 `r5_cross_session_claude_md_fires_on_3_sessions` FAIL — cross_session finding 미생성으로 `cs.len()==1` 불충족(현재 evaluate는 context_drift만).

- [ ] **Step 3: 노이즈 경로 헬퍼 + `claude_md_min_sessions` 필드 추가**

`r5_repeated_read.rs` 상단, `const TOKENS_PER_READ: u64 = 1200;` 아래에 추가:

```rust
/// §6.1 노이즈 경로 판정 — 생성/락 파일·의존/빌드 디렉터리는 CLAUDE.md 후보에서 제외.
fn is_noise_path(path: &str) -> bool {
    let lower = path.to_lowercase();
    let file = lower.rsplit(|c| c == '/' || c == '\\').next().unwrap_or("");
    if file.ends_with(".lock") || file.ends_with(".min.js")
        || file == "package-lock.json" || file == "cargo.lock"
    {
        return true;
    }
    const NOISE_DIRS: &[&str] = &["node_modules", "target", "dist", "build", ".git", "vendor"];
    // 경로 세그먼트 일치(부분 문자열 아님 — src/builder.rs 오제외 방지)
    lower.split(|c| c == '/' || c == '\\').any(|seg| NOISE_DIRS.contains(&seg))
}
```

`R5RepeatedRead` 구조체와 `Default`에 필드 추가:

```rust
pub struct R5RepeatedRead {
    pub drift_min_reads: u64, // 세션 내 한 파일의 드리프트 읽기 임계
    pub min_sessions: usize,  // 프로젝트에서 드리프트를 보인 세션 수 임계
    pub claude_md_min_sessions: usize, // §6.1 별개 세션 읽기 임계
}

impl Default for R5RepeatedRead {
    fn default() -> Self {
        R5RepeatedRead { drift_min_reads: 3, min_sessions: 2, claude_md_min_sessions: 3 }
    }
}
```

- [ ] **Step 4: cross_session 탐지 메서드 구현 + evaluate에서 호출**

`impl Rule for R5RepeatedRead` 블록 **위**에 `impl R5RepeatedRead` 블록을 추가:

```rust
impl R5RepeatedRead {
    /// §6.1 cross_session_claude_md — 프로젝트의 여러 세션에서 반복 읽히는 참조 파일.
    fn claude_md_findings(&self, store: &SqliteStore) -> Result<Vec<Finding>> {
        let mut stmt = store.conn.prepare(
            "SELECT COALESCE(host,''), COALESCE(project_id,''), session_id,
                    COALESCE(tool_kind,''), COALESCE(tool_target,'')
             FROM events
             WHERE kind='tool_call'
               AND tool_kind IN ('file_read','file_edit','file_write')
               AND COALESCE(tool_target,'') <> ''",
        )?;
        let rows: Vec<(String, String, String, String, String)> = stmt
            .query_map([], |r| Ok((r.get(0)?, r.get(1)?, r.get(2)?, r.get(3)?, r.get(4)?)))?
            .collect::<std::result::Result<_, _>>()?;

        // (host, project, target) → 읽기 세션 집합·읽기수·편집수
        #[derive(Default)]
        struct Agg {
            read_sessions: HashSet<String>,
            reads: u64,
            edits: u64,
        }
        let mut map: BTreeMap<(String, String, String), Agg> = BTreeMap::new();
        for (host, proj, sess, kind, target) in rows {
            if is_noise_path(&target) {
                continue;
            }
            let e = map.entry((host, proj, target)).or_default();
            match kind.as_str() {
                "file_read" => {
                    e.reads += 1;
                    e.read_sessions.insert(sess);
                }
                "file_edit" | "file_write" => e.edits += 1,
                _ => {}
            }
        }

        // 파일 자격(별개 세션≥임계 & 읽기우세) 판정 후 (host, project)별 묶기
        struct FileHit {
            path: String,
            sessions: Vec<String>,
        }
        let mut by_proj: BTreeMap<(String, String), Vec<FileHit>> = BTreeMap::new();
        for ((host, proj, target), agg) in map {
            if agg.read_sessions.len() >= self.claude_md_min_sessions && agg.reads > agg.edits {
                let mut sessions: Vec<String> = agg.read_sessions.into_iter().collect();
                sessions.sort_unstable();
                by_proj.entry((host, proj)).or_default().push(FileHit { path: target, sessions });
            }
        }

        let mut out = Vec::new();
        for ((host, project), mut files) in by_proj {
            if files.is_empty() {
                continue;
            }
            // 안정 정렬: 세션 수 desc, 경로 asc
            files.sort_by(|a, b| b.sessions.len().cmp(&a.sessions.len()).then(a.path.cmp(&b.path)));

            let est: u64 = files
                .iter()
                .map(|f| (f.sessions.len() as u64).saturating_sub(1) * TOKENS_PER_READ)
                .sum();

            // 프로젝트 전체 읽기 세션 합집합
            let mut all_sessions: Vec<String> =
                files.iter().flat_map(|f| f.sessions.iter().cloned()).collect();
            all_sessions.sort_unstable();
            all_sessions.dedup();
            let total_sessions = all_sessions.len();
            let capped_sessions: Vec<&str> =
                all_sessions.iter().take(100).map(|s| s.as_str()).collect();

            let files_json: Vec<serde_json::Value> = files
                .iter()
                .take(100)
                .map(|f| serde_json::json!({ "path": f.path, "session_count": f.sessions.len() }))
                .collect();
            let file_paths: Vec<&str> = files.iter().take(100).map(|f| f.path.as_str()).collect();

            let cwd = capped_sessions
                .first()
                .copied()
                .and_then(|sid| store.session_ctx(sid).ok().flatten())
                .and_then(|(_, _, cwd, _)| cwd);

            out.push(Finding {
                rule_id: "R5".into(),
                severity: Severity::Suggest,
                scope_host: Some(host.clone()),
                scope_project: Some(project.clone()),
                scope_kind: "project".into(),
                scope_ref: project.clone(),
                evidence: serde_json::json!({
                    "subtype": "cross_session_claude_md",
                    "user_actionability": "high",
                    "files": files_json,
                    "session_ids": capped_sessions,
                    "total_sessions": total_sessions,
                    "cwd": cwd,
                }),
                est_tokens_saved: est,
                prescription: Some(Prescription {
                    kind: "add_to_claude_md".into(),
                    payload: serde_json::json!({ "files": file_paths }),
                }),
                dedup_key: format!("R5|{host}|{project}|cross_session_claude_md"),
            });
        }
        Ok(out)
    }
}
```

그리고 `impl Rule for R5RepeatedRead`의 `evaluate` **맨 끝**(context_drift `out.push(...)` 루프가 닫힌 직후의 `Ok(out)`)을 cross_session 병합으로 교체한다. `claude_md_findings` 내부에도 `Ok(out)`이 있으니, **within_session_context_drift dedup_key로 끝나는 루프 다음의 그 `Ok(out)`**을 유일 앵커로 삼아 교체:

기존(evaluate 말미):
```rust
                dedup_key: format!("R5|{host}|{project}|within_session_context_drift"),
            });
        }
        Ok(out)
    }
}
```
교체:
```rust
                dedup_key: format!("R5|{host}|{project}|within_session_context_drift"),
            });
        }
        out.extend(self.claude_md_findings(store)?);
        Ok(out)
    }
}
```

- [ ] **Step 5: 테스트 통과 확인(신규 4개 + 기존 context_drift 회귀)**

Run: `cargo test -p agent-mentor r5`
Expected: PASS — 신규 `r5_cross_session_*` 4개 + 기존 `r5_pure_rereads_3x_two_sessions_fires`·`r5_edit_then_read_suppressed`·`r5_search_then_read_suppressed`·`r5_post_compaction_reread_suppressed`·`r5_needs_two_drift_sessions` 전부 green(기존 테스트의 파일은 세션 2개뿐이라 cross_session 미발화 → findings.len 불변).

- [ ] **Step 6: 커밋**

```bash
git add crates/core/src/rules/r5_repeated_read.rs
git commit -m "feat(R5): cross_session_claude_md subtype — 여러 세션 반복 읽기→CLAUDE.md 레버 (스펙 §6.1)"
```

---

### Task 2: finding_advice R5 subtype 분기 (서사 표면)

**Files:**
- Modify: `crates/core/src/diary/mod.rs` (`finding_advice`의 `"R5"` arm)
- Test: `crates/core/src/diary/mod.rs` (`#[cfg(test)] mod tests`), `crates/core/src/coach.rs` (`#[cfg(test)] mod tests`)

**Interfaces:**
- Consumes: Task 1의 cross_session evidence 키(`subtype`, `files:[{path}]`, `cwd`), `est_tokens_saved`.
- Produces: `finding_advice("R5", evidence, est)`가 subtype `cross_session_claude_md`이면 CLAUDE.md 문구, 아니면 기존 context_drift 문구.

- [ ] **Step 1: 실패 테스트 추가 (finding_advice cross_session)**

`crates/core/src/diary/mod.rs`의 tests 모듈, `finding_advice_r5_context_drift` 테스트 **뒤**에 추가:

```rust
    #[test]
    fn finding_advice_r5_cross_session_claude_md() {
        let (detail, action) = super::finding_advice(
            "R5",
            &serde_json::json!({
                "subtype": "cross_session_claude_md", "user_actionability": "high",
                "files": [{"path": "docs/architecture.md", "session_count": 4}],
                "session_ids": [], "total_sessions": 4,
                "cwd": "D:\\Project\\cowork"
            }),
            9600,
        );
        assert!(detail.contains("architecture.md")); // 반복 읽힌 파일 인용
        assert!(detail.contains("추정"));            // 잠재/추정 프레이밍(정밀도의 선)
        assert!(action.contains("CLAUDE.md"));       // CLAUDE.md 레버
        assert!(action.contains("cowork"));          // cwd로 어느 CLAUDE.md인지 지목
    }
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `cargo test -p agent-mentor finding_advice_r5_cross_session_claude_md`
Expected: FAIL — 현재 R5 arm은 subtype 무시하고 context_drift 문구만 생성해 `action.contains("CLAUDE.md")` 불충족.

- [ ] **Step 3: R5 arm에 subtype 분기 추가**

`crates/core/src/diary/mod.rs`의 `finding_advice`에서 기존 R5 arm을 아래로 교체:

기존:
```rust
        "R5" => {
            // v2.1: 프로젝트 집계 context_drift (§6.2). 구 세션 evidence(path/count)는 폐기됨.
            let n = evidence.get("total_sessions").and_then(|v| v.as_u64()).unwrap_or(0);
            (
                format!("몇몇 세션({n}건)에서 같은 파일을 편집 없이 여러 번 다시 읽었어요 (~{est_tokens_saved}토큰 잠재, 추정)"),
                "직접 시키신 게 아니라 작업 중 파일 구조 기억이 약해졌을 때 생겨요 — 다음엔 '먼저 관련 파일을 읽고 역할·수정 위치를 짧게 메모한 뒤 진행해'처럼 시작하면 반복 재확인이 줄어요".to_string(),
            )
        }
```
교체:
```rust
        "R5" => {
            // §6.1 cross_session_claude_md — 여러 세션 반복 읽기 → CLAUDE.md 레버
            if evidence.get("subtype").and_then(|v| v.as_str()) == Some("cross_session_claude_md") {
                let files = evidence.get("files").and_then(|v| v.as_array());
                let n_files = files.map(|a| a.len()).unwrap_or(0);
                let shown = files
                    .map(|a| {
                        a.iter()
                            .filter_map(|f| f.get("path").and_then(|p| p.as_str()))
                            .take(3)
                            .collect::<Vec<_>>()
                            .join("`, `")
                    })
                    .unwrap_or_default();
                let more = if n_files > 3 { format!(" 외 {}개", n_files - 3) } else { String::new() };
                let where_claude_md = match evidence.get("cwd").and_then(|v| v.as_str()) {
                    Some(cwd) => format!("`{cwd}\\CLAUDE.md`"),
                    None => "이 프로젝트의 `CLAUDE.md`".to_string(),
                };
                return (
                    format!("이 프로젝트 여러 세션에서 `{shown}`{more}를 반복해서 읽었어요 (최대 ~{est_tokens_saved}토큰 추정)"),
                    format!("이 파일들의 요약이나 포인터를 {where_claude_md}에 넣어두면 매 세션 다시 읽지 않아도 돼요"),
                );
            }
            // v2.1: 프로젝트 집계 context_drift (§6.2). 구 세션 evidence(path/count)는 폐기됨.
            let n = evidence.get("total_sessions").and_then(|v| v.as_u64()).unwrap_or(0);
            (
                format!("몇몇 세션({n}건)에서 같은 파일을 편집 없이 여러 번 다시 읽었어요 (~{est_tokens_saved}토큰 잠재, 추정)"),
                "직접 시키신 게 아니라 작업 중 파일 구조 기억이 약해졌을 때 생겨요 — 다음엔 '먼저 관련 파일을 읽고 역할·수정 위치를 짧게 메모한 뒤 진행해'처럼 시작하면 반복 재확인이 줄어요".to_string(),
            )
        }
```

- [ ] **Step 4: coach.rs fix_command None 확인 테스트 추가**

`crates/core/src/coach.rs`의 tests 모듈, `v2_aggregate_rules_have_no_fix_command` 테스트 안에 한 줄 추가(또는 인접):

```rust
        assert_eq!(
            fix_command("R5", &json!({"subtype": "cross_session_claude_md", "files": []})),
            None
        );
```

- [ ] **Step 5: 테스트 통과 확인(신규 + 기존 R5/coach 회귀)**

Run: `cargo test -p agent-mentor finding_advice_r5 && cargo test -p agent-mentor fix_command`
Expected: PASS — `finding_advice_r5_cross_session_claude_md`(신규) + `finding_advice_r5_context_drift`(기존, subtype이 within_session이라 else 분기 유지) + coach fix_command 테스트 green.

- [ ] **Step 6: 커밋**

```bash
git add crates/core/src/diary/mod.rs crates/core/src/coach.rs
git commit -m "feat(diary): finding_advice R5 subtype 분기 — cross_session CLAUDE.md 문구 (스펙 §6.1)"
```

---

### Task 3: 프론트 subtype별 카드 제목 (CoachTab)

**Files:**
- Modify: `src/lib/ui/coach-helpers.ts` (신규 `coachTitle`)
- Create: `src/lib/ui/coach-helpers.test.ts`
- Modify: `src/lib/ui/CoachTab.svelte` (로컬 TITLE 맵 제거 → `coachTitle` 사용)

**Interfaces:**
- Consumes: `CoachFinding`의 `rule_id: string`, `evidence: unknown`(Task 1의 `subtype`).
- Produces: `coachTitle(ruleId: string, evidence: unknown): string`.

- [ ] **Step 1: 실패 vitest 작성**

`src/lib/ui/coach-helpers.test.ts` 신규 생성:

```ts
import { describe, it, expect } from 'vitest';
import { coachTitle } from './coach-helpers';

describe('coachTitle', () => {
  it('R5 cross_session subtype → CLAUDE.md 카피', () => {
    expect(coachTitle('R5', { subtype: 'cross_session_claude_md' })).toContain('CLAUDE.md');
  });
  it('R5 context_drift subtype → 기존 다시읽기 제목', () => {
    expect(coachTitle('R5', { subtype: 'within_session_context_drift' })).toContain('다시 읽었어요');
  });
  it('R1은 기존 제목', () => {
    expect(coachTitle('R1', null)).toContain('MCP');
  });
  it('알 수 없는 rule → fallback', () => {
    expect(coachTitle('RX', {})).toBe('아낄 수 있는 게 보여요');
  });
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `npx vitest run src/lib/ui/coach-helpers.test.ts`
Expected: FAIL — `coachTitle` export 없음(import 에러).

- [ ] **Step 3: `coachTitle` 구현**

`src/lib/ui/coach-helpers.ts` 끝에 추가:

```ts
const COACH_TITLE: Record<string, string> = {
  R1: '안 쓰는 MCP 서버가 토큰을 먹고 있어요',
  R2: '안 쓰는 플러그인이 자리만 차지해요',
  R5: '세션 안에서 같은 파일을 여러 번 다시 읽었어요',
  R7: '이 프로젝트, 가벼운 작업엔 시작 모델을 낮춰보세요',
  R9: '웹 검색이 너무 잦아요',
  R10: '자동화 파이프라인이 Opus로 돌고 있어요',
  R11: '거부한 뒤 결국 허용한 도구가 있어요',
  R12: '설치해둔 스킬이 놀고 있어요',
};

function subtypeOf(evidence: unknown): string | null {
  if (typeof evidence !== 'object' || evidence === null) return null;
  const s = (evidence as Record<string, unknown>).subtype;
  return typeof s === 'string' ? s : null;
}

export function coachTitle(ruleId: string, evidence: unknown): string {
  if (ruleId === 'R5' && subtypeOf(evidence) === 'cross_session_claude_md') {
    return '여러 세션에서 반복해 읽는 파일 — CLAUDE.md에 넣어두면 좋겠어요';
  }
  return COACH_TITLE[ruleId] ?? '아낄 수 있는 게 보여요';
}
```

- [ ] **Step 4: vitest 통과 확인**

Run: `npx vitest run src/lib/ui/coach-helpers.test.ts`
Expected: PASS — 4개 케이스 green.

- [ ] **Step 5: CoachTab에서 coachTitle 사용**

`src/lib/ui/CoachTab.svelte`의 import에 `coachTitle` 추가(기존 coach-helpers import 라인에 병합), 로컬 `TITLE` 맵(78–87행) 제거, 헤더 제목을 교체:

기존:
```svelte
          <span class="title">{icon(f.severity)} {TITLE[f.rule_id] ?? '아낄 수 있는 게 보여요'}</span>
```
교체:
```svelte
          <span class="title">{icon(f.severity)} {coachTitle(f.rule_id, f.evidence)}</span>
```

(import 예: `import { sessionIdsOf, totalSessionsOf, ctxLine, coachTitle } from './coach-helpers';` — 기존 import 심볼에 `coachTitle`만 추가.)

- [ ] **Step 6: 빌드 + 전체 vitest 회귀 확인**

Run: `npm run build && npx vitest run`
Expected: 빌드 성공(타입 에러 0 — `TITLE` 참조 잔존 없음), vitest 전부 green(기존 + 신규 coach-helpers).

- [ ] **Step 7: 커밋**

```bash
git add src/lib/ui/coach-helpers.ts src/lib/ui/coach-helpers.test.ts src/lib/ui/CoachTab.svelte
git commit -m "feat(coach-ui): R5 subtype별 카드 제목 — cross_session은 CLAUDE.md 카피 (스펙 §8.2)"
```

---

## Post-merge (코드 태스크 아님 — 사용자 E2E)

- 실데이터 스캔(`cargo run -p agent-mentor -- rules` 또는 앱 스캔)에서 R5 카드가 **두 종류**로 뜨는지 육안: context_drift(세션 내 재읽기) + cross_session(프로젝트 반복 읽기 파일 → CLAUDE.md 제안). cross_session 카드에 파일 목록·cwd 기반 CLAUDE.md 경로가 정확히 표시되는지.
- 노이즈 경로(node_modules 등)·WIP 파일(편집 우세)이 cross_session에서 제외되는지.
